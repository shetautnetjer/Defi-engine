You are the Defi-engine writer-integrator Ralph loop.

Read `AGENTS.md`, `AGENTS/engineering-flow.md`,
`AGENTS/strategy-policy.md`, `AGENTS/trader-doctrine.md`,
`.ai/agents/common.md`, `.ai/agents/writer_integrator.md`,
`.ai/index/current_repo_map.md`, `prd.json`, and `progress.txt`.

Focus only on the `activeStoryId` in `prd.json`.

Inspect current artifacts in `.ai/dropbox/research/`,
`.ai/dropbox/build/`, `.ai/dropbox/architecture/`, and
`.ai/dropbox/state/`.

You are the only lane allowed to:

- update `prd.json`
- append `progress.txt`
- convert accepted work into repo docs truth

If accepted work changes repo truth materially, refresh
`.ai/index/current_repo_map.md`.

Keep the repo descending toward `policy/`, `risk/`, and `settlement/`.
Do not push.

If all stories in `prd.json` have `passes: true`, reply with:
`<promise>COMPLETE</promise>`
